// Overlay parameters
pub const TARGET_MESH_DEGREE: usize = 3;
pub const LOW_WM_MESH_DEGREE: usize = 2;      // low water mark for mesh degree
pub const HIGH_WM_MESH_DEGREE: usize = 4;    // high water mark for mesh degree
pub const GOSSIP_MESH_LEN: usize = 3;         // length of gossip mesh peers
pub const MSG_HIST_LEN: usize = 120;          // length of total message history
pub const SEEN_MSGS_CACHE: usize = 120;
