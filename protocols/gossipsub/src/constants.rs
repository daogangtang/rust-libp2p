// Overlay parameters
pub const TARGET_MESH_DEGREE: u32 = 3;
pub const LOW_WM_MESH_DEGREE: u32 = 2;      // low water mark for mesh degree
pub const HIGH_WM_MESH_DEGREE: u32 = 4;    // high water mark for mesh degree
pub const GOSSIP_MESH_LEN: u32 = 3;         // length of gossip mesh peers
pub const MSG_HIST_LEN: u32 = 120;          // length of total message history
pub const SEEN_MSGS_CACHE: u32 = 120;
